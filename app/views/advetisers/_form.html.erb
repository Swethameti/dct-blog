<%= form_for(@advetiser) do |f| %>
  <% if @advetiser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@advetiser.errors.count, "error") %> prohibited this advetiser from being saved:</h2>

      <ul>
      <% @advetiser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
